<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!--css style link-->
    <link rel="stylesheet" href="/css/app.css">
    <!--bootstrap link-->
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <!-- JavaScript Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <style>
        .bd-placeholder-img {
            font-size: 1.125rem;
            text-anchor: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            user-select: none;
        }

        @media (min-width: 768px) {
            .bd-placeholder-img-lg {
                font-size: 3.5rem;
            }
        }
        </style>

    <title>Library</title>
</head>
<body>
    <div class="nav-container navbar-dark bg-dark">
        <div class="navbar">
            <div class="nav-brand">
                <a href="/">Library</a>
            </div>
            <div class="nav-list">
                <%if(user.role === 'Student'){%>
                    <a href="/dashboard">Dashboard</a>

                <%}else if(user.role === 'admin'){%>
                    <a href="/add">Add Books</a>
                <%}%>

                <a href="/logout">logout</a>
            </div>
        </div>
    </div>

<form action="/search" method="post">
    <div class="nav-scroller bg-body shadow-sm">
        <nav class="nav nav-underline" aria-label="Secondary navigation">
            <%if(user.role == "admin"){%>
                <a class="nav-link active fs-6" aria-current="page" href="/adminDashboard">Dashboard</a>
            <%}%>
        <%if(user.role == "Student"){%>
            <a class="nav-link fs-6" href="#">
                Categories
                <span class="badge bg-light text-dark rounded-pill align-text-bottom">6</span>
            </a>
        <%}else{%>
            <a class="nav-link fs-6" href="/register">
                Register Student
            </a>
        <%}%>

        <button type="submit" name="navLink" value = "Software"  class="nav-link fs-6">Software</button>
        <button type="submit" name="navLink" value = "Web"       class="nav-link fs-6">Web</button>
        <button type="submit" name="navLink" value = "Android"   class="nav-link fs-6">Android</button>
        <button type="submit" name="navLink" value = "Operating" class="nav-link fs-6">Operating</button>
        <button type="submit" name="navLink" value = "Networks"  class="nav-link fs-6">Networks</button>
        <button type="submit" name="navLink" value = "Database"  class="nav-link fs-6">Database</button>
        <button type="submit" name="navLink" value = "Cloud Computing"  class="nav-link fs-6">Cloud Computing</button>
        </nav>
    </div>
</form>
